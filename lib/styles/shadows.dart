import 'package:flutter/material.dart';

class AppShadows {
  /// our shadow colour for our whit components
  static const boxShadow = BoxShadow(
    color: Color(0xff16205B),
    offset: Offset(20, 0),
    blurRadius: 20,
    spreadRadius: 0,
  );

  AppShadows._();
}
